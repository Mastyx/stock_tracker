[package]
name = "stock_tracker"
version = "0.1.0"
edition = "2024"

[dependencies]
macroquad = "0.4"
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
